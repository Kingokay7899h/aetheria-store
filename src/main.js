import React from 'react'
import ReactDOM from 'react-dom/client'
import AetheriaClone from './App.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <AetheriaClone />
  </React.StrictMode>,
)
